<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>Label</key>
	<string>com.brandcomposer.calendar</string>

	<key>ProgramArguments</key>
	<array>
		<string>/bin/bash</string>
		<string>/Users/jackwatkins/brandcomposer-calendar/deploy-calendar.sh</string>
	</array>

	<!-- Sunday 08:00 local. The GitHub Action runs Sunday 00:00 UTC, so the
	     regenerated script is already pushed by the time this fires. -->
	<key>StartCalendarInterval</key>
	<dict>
		<key>Weekday</key>
		<integer>0</integer>
		<key>Hour</key>
		<integer>8</integer>
		<key>Minute</key>
		<integer>0</integer>
	</dict>

	<!-- Run on wake/login if the scheduled time was missed. -->
	<key>RunAtLoad</key>
	<false/>

	<!-- A LaunchAgent runs as the user and cannot write to /var/log. -->
	<key>StandardOutPath</key>
	<string>/Users/jackwatkins/Library/Logs/brandcomposer-calendar.log</string>

	<key>StandardErrorPath</key>
	<string>/Users/jackwatkins/Library/Logs/brandcomposer-calendar.error.log</string>
</dict>
</plist>
